import { FilterTransactionPipe } from './filter-transaction.pipe';

describe('FilterTransactionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTransactionPipe();
    expect(pipe).toBeTruthy();
  });
});
